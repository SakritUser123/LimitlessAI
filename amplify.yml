version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  env:
    variables:
      CLERK_SECRET_KEY: sk_live_xxx
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_live_xxx
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*